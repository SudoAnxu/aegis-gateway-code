# Environment capture

Populate this file from the machine used for the final experiment. Do not hand-invent values.

```text
Commit/tag:
Benchmark version:
Benchmark SHA-256:
OS:
Architecture:
CPU:
RAM:
Go version:
Python version:
Docker version:
Repetitions:
RNG seeds:
```

The final reproducibility record should be generated or filled from the actual environment immediately before the final paper artifact is frozen.
